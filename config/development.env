# Development Environment Configuration
# This file defines ALL environment variables for local development
# Used by: Local development on your laptop
#
# This mirrors your existing .env file for consistency
#
# Last Updated: October 27, 2025

# ============================================
# Core Application Configuration
# ============================================

# Development mode (disables auth, enables debugging)
NODE_ENV=development

# Local development port (only for local dev, Cloud Run sets this automatically)
PORT=3000

# ============================================
# Database Configuration (PostgreSQL)
# ============================================

# Local PostgreSQL database
DB_USER=ryanhaines
DB_HOST=localhost
DB_NAME=legal_forms_db
DB_PASSWORD=
DB_PORT=5432

# ============================================
# Python Normalization Pipeline Configuration
# ============================================

# Local Python service (run with: cd "normalization work" && uvicorn main:app)
PIPELINE_API_URL=http://localhost:8000
PIPELINE_API_ENABLED=true
PIPELINE_API_TIMEOUT=60000
PIPELINE_API_KEY=
EXECUTE_PIPELINE_ON_SUBMIT=true
CONTINUE_ON_PIPELINE_FAILURE=true

# ============================================
# Dropbox Configuration
# ============================================

# Dropbox disabled in local development (use test folder if needed)
DROPBOX_ENABLED=false
DROPBOX_ACCESS_TOKEN=
DROPBOX_BASE_PATH=/Apps/LegalFormApp
LOCAL_OUTPUT_PATH=/output
CONTINUE_ON_DROPBOX_FAILURE=true

# ============================================
# Email Notification Configuration
# ============================================

# Email disabled in local development (or use test API key)
EMAIL_PROVIDER=sendgrid
EMAIL_FROM_ADDRESS=notifications@liptonlegal.com
EMAIL_FROM_NAME=Lipton Legal
EMAIL_ENABLED=false
EMAIL_MAX_RETRIES=3
EMAIL_RETRY_DELAY_MS=1000
SENDGRID_API_KEY=

# ============================================
# Google Cloud Platform Configuration
# ============================================

# GCP features disabled in local development
GCLOUD_PROJECT=docmosis-tornado
GCS_BUCKET_NAME=docmosis-tornado-form-submissions

# ============================================
# Authentication Configuration
# ============================================

# No authentication required in development mode
ACCESS_TOKEN=dev-token-not-required

# ============================================
# Notes
# ============================================
# In development mode (NODE_ENV=development):
# - Authentication is BYPASSED (no token required)
# - Cloud Storage is DISABLED (uses local filesystem)
# - Most external services can be disabled for faster development
#
# To enable services for testing:
# - Set EMAIL_ENABLED=true and add your SendGrid test API key
# - Set DROPBOX_ENABLED=true and add your Dropbox token
# - Run Python pipeline locally: cd "normalization work" && uvicorn main:app
